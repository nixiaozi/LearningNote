


对于同一对象的增删改查可以统一合并到一个页面，并且使用条件判断以显示不同的数据。
对于需要使用的前端可重复使用Html的代码模块，可以写成ParialView然后通过调用部分页视图加载。
对于需要输出的html字符串可以使用view视图，通过控制器拿到输出然后传递给后台
使用ViewBag统一类别实例：
	包括当前页面的主要操作：是添加，修改或仅查看
	包括一个Data属性用来传递Model数据






	商城规则：
	付款和订单需要分开表进行存储
	建立一个错误处理Service，使用回调方式处理错误
	独立出专门的模板服务用来模板请求
	Class的属性中是否可以使用await异步获取数据
	
	使用日志等级：
	1、添加性能耗时测试+应用等级